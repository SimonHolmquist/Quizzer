<Window x:Class="Quizzer.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Quizzer" Width="1100" Height="750"
        WindowStartupLocation="CenterScreen">
    <DockPanel>
        <Border DockPanel.Dock="Top" Padding="12" BorderThickness="0,0,0,1" BorderBrush="#333">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Text="Quizzer" FontSize="20" FontWeight="SemiBold"/>

                <TextBlock Grid.Column="1" Text="{Binding StatusText}" Opacity="0.8" VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <ContentControl Content="{Binding Nav.Current}" />
    </DockPanel>
</Window>
